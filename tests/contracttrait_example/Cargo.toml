[package]
name = "test_contracttrait_example"
version.workspace = true
authors = ["Stellar Development Foundation <info@stellar.org>"]
license = "Apache-2.0"
edition = "2021"
publish = false
rust-version.workspace = true

[lib]
crate-type = ["cdylib"]
doctest = false

[features]
testutils = []

[dependencies]
soroban-sdk = { path = "../../soroban-sdk" }
test_contracttrait_lib = { path = "../contracttrait_lib" }

[dev-dependencies]
soroban-sdk = { path = "../../soroban-sdk", features = ["testutils"] }
test_contracttrait_lib = { path = "../contracttrait_lib" }
