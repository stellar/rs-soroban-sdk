[workspace]
resolver = "2"

members = [
    "soroban-sdk",
    "soroban-sdk-macros",
    "soroban-meta",
    "soroban-spec",
    "soroban-spec-rust",
    "soroban-ledger-snapshot",
    "soroban-token-sdk",
    "soroban-token-spec",
    "stellar-asset-spec",
    "tests/*",
]

[workspace.package]
version = "28.0.0-rc.1"
rust-version = "1.91.0"

[workspace.dependencies]
soroban-sdk = { version = "28.0.0-rc.1", path = "soroban-sdk" }
soroban-sdk-macros = { version = "28.0.0-rc.1", path = "soroban-sdk-macros" }
soroban-meta = { version = "28.0.0-rc.1", path = "soroban-meta" }
soroban-spec = { version = "28.0.0-rc.1", path = "soroban-spec", default-features = false }
soroban-spec-rust = { version = "28.0.0-rc.1", path = "soroban-spec-rust" }
soroban-ledger-snapshot = { version = "28.0.0-rc.1", path = "soroban-ledger-snapshot" }
soroban-token-sdk = { version = "28.0.0-rc.1", path = "soroban-token-sdk" }
soroban-token-spec = { version = "28.0.0-rc.1", path = "soroban-token-spec" }
stellar-asset-spec = { version = "28.0.0-rc.1", path = "stellar-asset-spec" }

[workspace.dependencies.soroban-env-common]
version = "=29.0.0"
#git = "https://github.com/stellar/rs-soroban-env"
#rev = "7fb0a840812fe8921bb48bebf7b4aa7160467ec8"

[workspace.dependencies.soroban-env-guest]
version = "=29.0.0"
#git = "https://github.com/stellar/rs-soroban-env"
#rev = "7fb0a840812fe8921bb48bebf7b4aa7160467ec8"

[workspace.dependencies.soroban-env-host]
version = "=29.0.0"
#git = "https://github.com/stellar/rs-soroban-env"
#rev = "7fb0a840812fe8921bb48bebf7b4aa7160467ec8"

[workspace.dependencies.stellar-strkey]
version = "=0.0.16"

[workspace.dependencies.stellar-xdr]
version = "=28.0.0"
#git = "https://github.com/stellar/rs-stellar-xdr"
#rev = "a749b69b3471aec0c20ec431b0297f3414d66421"
default-features = false

[patch.crates-io]
# Pending https://github.com/stellar/rs-stellar-xdr/pull/580, which adds the
# const-only `*Const` types and their const XDR serializers that the contract
# spec generation in soroban-sdk-macros builds on.
stellar-xdr = { git = "https://github.com/stellar/rs-stellar-xdr", rev = "e384b9b143bd7f11dd724f068f1a373f31aa69a4" }
# Pending https://github.com/stellar/rs-soroban-env/pull/1733, which adapts the
# host to the XDR constants that rs-stellar-xdr#580 changes from u64 to u32.
# Only the 29.x line carries that adaptation, so the test ledger's default
# protocol is 29 until it is released.
soroban-env-common = { git = "https://github.com/stellar/rs-soroban-env", rev = "de68ee914401e70d3d8350a4d004c0442943324e" }
soroban-env-guest = { git = "https://github.com/stellar/rs-soroban-env", rev = "de68ee914401e70d3d8350a4d004c0442943324e" }
soroban-env-host = { git = "https://github.com/stellar/rs-soroban-env", rev = "de68ee914401e70d3d8350a4d004c0442943324e" }
#soroban-env-common = { path = "../rs-soroban-env/soroban-env-common" }
#soroban-env-guest = { path = "../rs-soroban-env/soroban-env-guest" }
#soroban-env-host = { path = "../rs-soroban-env/soroban-env-host" }
#[patch."https://github.com/stellar/rs-stellar-xdr"]
#stellar-xdr = { path = "../rs-stellar-xdr/" }

[profile.dev]
overflow-checks = true
panic = "abort"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true

[profile.release-with-logs]
inherits = "release"
debug-assertions = true

# The release-without-lto profile is useful for testing build cases when lto
# are disabled, such as those like the test_zero test vector. See the test_zero
# README for more details.
[profile.release-without-lto]
inherits = "release"
lto = "off"
